allCategories:
SELECT * FROM category;

upsertCategory:
INSERT INTO category (id, label, color, created, lastModified) VALUES (
    :id, :label, :color, :created, :lastModified
);

deleteCategory:
DELETE FROM category WHERE id = ?;
